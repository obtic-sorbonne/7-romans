[project]
name = "7romans"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "accelerate>=0.22.0",
    "pydantic>=2.9.2",
    "renard-pipeline>=0.5.0",
]
